const saldo = 1;
const unidade = (saldo === 1 ? "real" : "reais"); // ternario, "?"condição, ":" caso seja false
console.log(`Voce tem R$${saldo} ${unidade}`);

//if(saldo ===1){
//    unidade ="real"
//}else{
//    unidade ="reais"
//}


//if (saldo === 1) {
//    console.log(`Voce tem R$${saldo} real`);
//} else {
//   console.log(`Voce tem R$${saldo} reais`);
//}